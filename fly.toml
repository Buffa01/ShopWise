app = "shopwise-api"
primary_region = "gru"
kill_signal = "SIGINT"
kill_timeout = "5s"

[build]
  dockerfile = "Dockerfile"

[deploy]
  release_command = "pnpm --filter @shopwise/api prisma:deploy"

[env]
  API_PORT = "8080"
  NODE_ENV = "production"
  SHORT_LINK_BASE_URL = "https://sw.shopwise.uy"
  STORAGE_DRIVER = "r2"
  R2_BUCKET = "shopwise-assets-production"
  R2_ENDPOINT = "https://7f87a1cd149109638b49567feefdeb28.r2.cloudflarestorage.com"
  STORAGE_TOTAL_LIMIT_BYTES = "9000000000"
  STORAGE_MAX_OBJECT_BYTES = "52428800"
  RATE_LIMIT_REDIRECT_WINDOW_MS = "60000"
  RATE_LIMIT_REDIRECT_MAX = "120"
  RATE_LIMIT_AUTH_WINDOW_MS = "60000"
  RATE_LIMIT_AUTH_MAX = "20"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

  [http_service.concurrency]
    type = "requests"
    hard_limit = 200
    soft_limit = 150

  [[http_service.checks]]
    interval = "15s"
    timeout = "2s"
    grace_period = "10s"
    method = "GET"
    path = "/v1/health"

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
